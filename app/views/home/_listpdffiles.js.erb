var s='<%= raw  shtml %>';

div = $("<div id='fullscreenlistpdffiles' class='fullscreen'>").html(s);
//$("body").html(div);
$("body").append(div);

// $('#misdocumentos').append(tb +  ck   + lg + sc + scp);

